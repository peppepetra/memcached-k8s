# Copyright 2021 Canonical ltd.
# See LICENSE file for licensing details.
#

restart:
  description: Restart memcached and flush cache content.

get-stats:
  description: Return memcached stats. Pass argument `settings=true` to return settings
  params:
    settings:
      description: Return stats settings
      type: boolean
      default: false
